import { filmApi } from '../API/filmApi'

let initialState = {
    
};

const topFilmsReducer = (state=initialState, action) => {
    switch (action.type){
        default:
            return state;
    }
}

export default topFilmsReducer;